#!groovy
pipeline {
    agent none
   stages {     
    stage('Build-Docker-Image') {
      steps {
        container('docker') {
          sh 'docker build -t SelJenkins:latest .'
        }
      }
    }
   }
 }
